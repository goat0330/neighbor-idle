<view class="page">
  <view class="section">
    <view class="section-title">实物图片 <text class="required">*</text><text class="count">{{images.length}}/9</text></view>
    <view class="img-grid">
      <view class="img-item" wx:for="{{images}}" wx:key="*this">
        <image src="{{item}}" mode="aspectFill" data-index="{{index}}" bindtap="previewImage" />
        <view class="img-del" data-index="{{index}}" catchtap="removeImage">×</view>
      </view>
      <view class="img-add" wx:if="{{images.length < 9}}" bindtap="chooseImage">
        <text class="add-icon">＋</text>
        <text class="add-text">添加图片</text>
      </view>
    </view>
  </view>

  <view class="section">
    <view class="form-item">
      <text class="label">标题 <text class="required">*</text></text>
      <input class="input" placeholder="物品标题" maxlength="30" value="{{form.title}}" bindinput="onTitle" />
    </view>
    <view class="form-item">
      <text class="label">描述</text>
      <textarea class="textarea" placeholder="描述物品情况" maxlength="500" value="{{form.desc}}" bindinput="onDesc" auto-height />
    </view>
    <view class="form-item">
      <text class="label">价格 <text class="required">*</text></text>
      <view class="price-wrap">
        <text class="rmb">¥</text>
        <input class="input" type="digit" placeholder="0" value="{{form.price}}" bindinput="onPrice" disabled="{{form.free}}" />
      </view>
    </view>
    <view class="form-item" wx:if="{{!form.free}}">
      <text class="label">原价</text>
      <view class="price-wrap">
        <text class="rmb">¥</text>
        <input class="input" type="digit" placeholder="选填" value="{{form.originalPrice}}" bindinput="onOriginalPrice" />
      </view>
    </view>
    <view class="form-item switch-item">
      <text class="label">免费送</text>
      <switch color="#07c160" checked="{{form.free}}" bindchange="onFreeChange" />
    </view>
    <view class="form-item">
      <text class="label">成色</text>
      <picker range="{{conditions}}" range-key="name" value="{{conditionIndex}}" bindchange="onConditionChange">
        <view class="picker-value">{{conditions[conditionIndex].name}} ▾</view>
      </picker>
    </view>
    <view class="form-item">
      <text class="label">分类</text>
      <picker range="{{categories}}" range-key="name" value="{{categoryIndex}}" bindchange="onCategoryChange">
        <view class="picker-value">{{categories[categoryIndex].name}} ▾</view>
      </picker>
    </view>
    <view class="form-item">
      <text class="label">联系电话</text>
      <input class="input" type="number" placeholder="选填" maxlength="11" value="{{form.phone}}" bindinput="onPhone" />
    </view>
  </view>

  <view class="submit-wrap safe-bottom">
    <button class="btn-primary" bindtap="submit" disabled="{{submitting}}">保存修改</button>
  </view>
</view>
